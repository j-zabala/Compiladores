class {
        int i(int x) {
                return x + 1;

        void main() { /*falta llave que cierre la funcion anterior*/
                int y;
                y = 4;
                if (y == 1) then {
                                return 1;
                        } else {
                                return inc(y);
                        }
        }
}
