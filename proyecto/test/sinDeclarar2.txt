class {
        int i(int x) {
                return x + 1;
        }
        void main() {
                if (y == 1) then {
                                return 1;
                        } else {
                                return inc(y);
                        }
        }
}
