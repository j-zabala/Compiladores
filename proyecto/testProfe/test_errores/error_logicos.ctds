class{

int promedio (int nota1, int nota2, int nota3){

	if ((nota1 > nota3) && (nota2 > nota3))then{
		return (nota1 + nota2)/30;
	}
	if ((nota1 > nota2) && (nota3 > nota2))then{
		return (nota1 + nota3)/30;
	}
	if ((nota2 > true) && (nota3 > nota1))then{ //error comparaciones deben ser del mismo tipo
		return (nota3 + nota2)/30;
	}
	if (!(nota1 == nota2) && (nota1 != nota3) && !(nota2 == nota3))then{
		return nota1;
	}
}



/*
Saca el promedio entre las dos notas mas altas
*/

int main(){
    return 1;
}

}
